name: Socket Scanning Demo
on:
  workflow_dispatch:  # Manual trigger only - DO NOT enable other triggers
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: per0x1de-1337/pullpilot@one
